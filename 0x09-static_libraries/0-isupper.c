#include "main.h"

/**
 * isupper - change string to upper case
 *
 * Return: string
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))              

		                return (1);                        

	        else                                       

			                return (0);
}
